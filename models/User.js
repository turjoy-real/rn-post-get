class User {
    constructor(id, userName, age, address, activity) {
        this.id = id;
        this.userName = userName;
        this.age = age,
        this.address = address,
        this.activity = activity
    }
}

export default User;